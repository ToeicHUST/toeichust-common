import { UpstashQstashEventPublisherPort } from './upstash-qstash-event-publisher.port';

describe('UpstashQstashEventPublisherPort', () => {
  it('should be defined', () => {
    expect(new UpstashQstashEventPublisherPort()).toBeDefined();
  });
});
